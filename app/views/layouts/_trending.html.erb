<div class="trending-assets-section p-5">
  <div class="container">
    <div class="column">
      <h2>Cryptocurrencies Trending Now</h2>
      <div class="card">
        <% @holdings.where(asset_type: "crypto").first(5).each do |holding|%>
         <% symbols = ["📈", "🔥", "🎇", "❇️"] %>
           <ul class="d-flex justify-content-between"><ol><%= symbols.sample %><%= holding.name %> </ol>
        <ol>$ <%= holding.price %></ol>
        </ul>
        <% end %>
      </div>
      <!-- Additional cards for cryptocurrencies -->
    </div>
    <div class="column">
      <h2>Stocks Trending Now</h2>
      <div class="card">
        <% @holdings.where(asset_type: "fiat").first(5).each do |holding|%>
        <% symbols = ["🔥", "💯", "💥", "💰", "💴"] %>
        <ul class="d-flex justify-content-between"><ol><%= symbols.sample %><%= holding.name %> </ol>
        <ol>$ <%= holding.price %></ol>
        </ul>
        <% end %>
      </div>
      <!-- Additional cards for stocks -->
    </div>
  </div>
</div>
